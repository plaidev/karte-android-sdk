name: Create Tag
on:
  workflow_dispatch:
    inputs:
      module:
        description: 'Module to create tag for'
        required: true
        type: choice
        options:
          - core
          - debugger
          - gradle-plugin
          - inbox
          - inappframe
          - inappmessaging
          - notifications
          - variables
          - visualtracking
      dry_run:
        description: 'Skip tag push (dry-run mode)'
        required: false
        type: boolean
        default: false
jobs:
  create-tag:
    # Avaliable only on master branch when `dry-run = false`.
    if: github.repository != 'plaidev/karte-android-sdk' && (github.event.inputs.dry_run == 'true' || github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Generate GitHub App token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: app-token
        with:
          # TODO: app-id is deprecated in v3.1.0, rename to client-id
          app-id: ${{ secrets.APP_TEAM_GITHUB_APP_ID }}
          private-key: ${{ secrets.APP_TEAM_GITHUB_APP_PRIVATE_KEY }}
          owner: plaidev
          repositories: |
            karte-android-sdk
            karte-android-dev
      - name: Configure Git
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          git config --global url."https://x-access-token:${GH_TOKEN}@github.com/plaidev/karte-android-sdk".insteadOf "https://github.com/plaidev/karte-android-sdk"
          git config --global url."https://x-access-token:${GH_TOKEN}@github.com/plaidev/karte-android-dev".insteadOf "https://github.com/plaidev/karte-android-dev"
          git config --global user.name "github actions"
          git config --global user.email "git@users.noreply.github.com"
      - name: Setup remote repository
        run: git remote add sync_repo https://github.com/plaidev/karte-android-sdk.git
      - name: Generate tag name
        id: tag
        env:
          MODULE: ${{ github.event.inputs.module }}
        run: |
          VERSION=$(cat ${MODULE}/version | tr -d '\n')
          echo "name=${MODULE}-${VERSION}" >> $GITHUB_OUTPUT
      - name: Check if tag exists
        env:
          TAG: ${{ steps.tag.outputs.name }}
        run: |
          if git ls-remote --tags origin | grep -q "refs/tags/${TAG}$"; then
            echo "Error: Tag ${TAG} already exists on origin"
            exit 1
          fi
          if git ls-remote --tags sync_repo | grep -q "refs/tags/${TAG}$"; then
            echo "Error: Tag ${TAG} already exists on sync_repo"
            exit 1
          fi
      - name: Create and push tag
        if: github.event.inputs.dry_run != 'true'
        env:
          TAG: ${{ steps.tag.outputs.name }}
        run: |
          git tag "${TAG}"
          git push origin "${TAG}"
          git push sync_repo "${TAG}"
      - name: Create tag (dry-run)
        if: github.event.inputs.dry_run == 'true'
        env:
          TAG: ${{ steps.tag.outputs.name }}
        run: |
          git tag "${TAG}"
          git push --dry-run origin "${TAG}"
          git push --dry-run sync_repo "${TAG}"
